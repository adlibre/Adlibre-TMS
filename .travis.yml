language: python

python:
  - "2.7"

env:
  - SAASU_WSACCESS_KEY=xxx

# command to install dependencies
#install: "pip install --allow-external py-dom-xpath --allow-unverified py-dom-xpath ."

# command to run tests
script: python manage.py test
